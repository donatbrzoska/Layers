using System;
using NUnit.Framework;
using UnityEngine;

public class TestRakel
{
    [Test]
    public void _()
    {
        //Rakel r = new Rakel(2, 1);
        //WorldSpaceCanvas wsc = new WorldSpaceCanvas(3, 3, new Vector3(0, 0, 0), 1);

        //Vector2Int pixel = wsc.MapToPixel(new Vector3(-1.5001f, 0, 0));

        //Assert.AreEqual(
        //    new Vector2Int(int.MinValue, int.MinValue),
        //    pixel
        //);
    }
}
